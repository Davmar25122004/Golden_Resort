package com.example.supabase.repository;

import com.example.supabase.domain.TurnoPerfil;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TurnoPerfilRepository extends JpaRepository<TurnoPerfil, Long> {}
